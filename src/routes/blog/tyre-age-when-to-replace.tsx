import { createFileRoute } from "@tanstack/react-router";
import TyreAgeWhenToReplace from "@/pages/blog/TyreAgeWhenToReplace";

export const Route = createFileRoute("/blog/tyre-age-when-to-replace")({
  component: TyreAgeWhenToReplace,
});
